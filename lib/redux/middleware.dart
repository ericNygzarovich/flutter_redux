import 'package:flutter/material.dart';
import 'package:redux/redux.dart';
import 'package:work_with_rest_api/redux/action.dart';

import 'store.dart';

// Future<void> loaderMiddleware(
//     Store<AppStorage> store, dynamic action, NextDispatcher nextDispach) async {
//   if (action is LoadImage) {
//     _getImageUrl('assets/avatar.jpeg')
//         .then((value) => store.dispatch(LoadedImage(imagePath: value)));
//   }

//   nextDispach(action);
// }

